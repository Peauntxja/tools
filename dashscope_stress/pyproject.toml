[project]
name = "dashscope-stress"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
pythonVersion = "3.10"
